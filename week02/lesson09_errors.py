# ============================================================
# УРОК 9 — Обработка ошибок (try / except)
# ============================================================
# Идея: "попробуй (try) выполнить опасный код; если он споткнётся —
# не падай, а выполни запасной план (except)".
# Проверку делает сам Python в момент падения — ты только стелешь соломку.
#
# Синтаксис:
#   try:
#       опасный код
#   except ТипОшибки:
#       запасной план
#
# Частый тип ошибки: ValueError — "неправильное значение"
# (например, попытка превратить "двадцать" в число через int()).
#
# ------------------------------------------------------------
# ПРИМЕР (для справки, разобрали в теории):
#
# try:
#     age = int(input("Введите возраст: "))
#     print(f"Через год вам будет {age + 1}")
# except ValueError:
#     print("Ошибка: нужно ввести число, а не текст")
#
# print("Программа продолжает работать дальше")
# ------------------------------------------------------------


# ============================================================
# ЗАДАЧА
# ============================================================
# 1. Спроси у пользователя число (input).
# 2. В блоке try: преврати его в int и выведи это число, умноженное на 2.
# 3. В блоке except ValueError: выведи "Это не число!"
#
# Ожидаемый вывод (два прогона):
#   Ввод: 5
#   Результат: 10
#
#   Ввод: привет
#   Это не число!
#
# Дисциплина: запусти ДВАЖДЫ (с числом и с текстом), сверь оба случая с эталоном.

# твой код:

# num = input('Введите число ')
# try:
#     print(f'Результат: {int(num) * 2}')
# except ValueError:
#     print('Это не число!')


data = ["10", "20", "привет", "30", "abc"]
total = 0
for num in data:
    try:
        total = total + int(num)
    except ValueError:
        pass
print(f"Сумма чисел: {total}")