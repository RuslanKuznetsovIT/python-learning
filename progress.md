# Руслан — Python Journey

## Профиль
- 33 года, Уфа
- Руководитель отдела продаж (ARTGK)
- Цель: backend разработчик + AI интеграции
- Целевой рынок: международный (remote)
- Время: 1–1.5 часа в день

## Сильные стороны
- Продуктовое мышление (придумал "Сторож")
- Работает с Claude как инструментом ежедневно
- Понимает бизнес-задачи и зачем нужен тот или иной инструмент
- Стратегически мыслит (не хочет учить то, что заменит AI)

## Слабые стороны / Что прокачать
- Код руками не писал — старт с нуля
- Английский слабый — развивать параллельно
- Математика средняя

## Стек (цель)
Python → FastAPI → PostgreSQL → AI интеграции (Claude API) → Docker → деплой

## Дорожная карта

| Период | Тема |
|--------|------|
| Месяц 1–2 | Python основы: переменные, типы, условия, циклы, функции |
| Месяц 3–4 | Структуры данных, файлы, исключения, ООП основы |
| Месяц 5–6 | FastAPI, базы данных, SQL |
| Месяц 7–9 | Первые реальные проекты, AI API |
| Месяц 10–14 | Портфолио, тестирование, выход на международный рынок |

## Прогресс

### Неделя 1
- [x] Урок 1: переменные, типы данных, print
- [x] Урок 2: ввод данных, арифметика
- [x] Урок 3: условия if/elif/else — оба задания сделаны (возрастные категории + бизнес-логика МОП/ПМ)
- [x] Урок 3.5 (пересборка): f-строки — ОСВОЕНО. 5 упражнений + тест с подменой переменной (`manager_name = "Артём"` → `"Ivan"`). Файл: `lesson03_5_fstrings_v2.py`.
- [x] Урок 4a: цикл while — ОСВОЕН (19.05.2026). Все 3 задания выполнены самостоятельно: обратный отсчёт 5→1, план продаж 10 сделок с f-строками, сумма 1..100 = 5050. Файл: `lesson04a_while.py`.
- [x] Урок 4a ДЗ (самопроверка, 20.05.2026): Сумма 1..100 + Комиссия менеджера. Файл: `lesson04a_homework.py`. Задача 1 — с первого прогона. Задача 2 — баг 15% (`* 1.5` вместо `* 0.15`), нашёл и исправил сам после подсказки «смотри на пример из ТЗ». Все три теста (80к, 150к, 100к-граница) совпали с эталоном.
- [x] Урок 4b: цикл for + range — ОСВОЕН (20.05.2026). Файл: `lesson04b_for.py`. Задача 1 (таблица умножения на 7) — рабочее но переусложнённое решение (лишний `n`, `i *= 7`). Задача 2 (сумма чётных) — сделал оба варианта; первый имел баг (`range(1, 101, 2)` → нечётные = 2500 вместо 2550), нашёл и исправил сам пока работали над Задачей 3. Задача 3 (план продаж) — первая «стена», написал «не понимаю». Пройдена через мини-упражнение «прокрути цикл руками» → разблокировал паттерн накопителя за 30 сек. Финал: вывод 1-в-1 с эталоном, итог 350000.
- [x] Урок 4b бонусная: `range` с отрицательным шагом + `end=" "` — ОСВОЕНО (21.05.2026).

### Неделя 2 (май–июнь 2026)
- [x] Урок 5 — функции (`def/return`) — ОСВОЕН (22–26.05.2026). Разбит на 3 сессии: теория + разогрев (22 мая), закрепление циклов 5 задач (25 мая), повтор def + 4 задачи на функции (26 мая). Все 4 задачи решены самостоятельно без подсказок. Задача 4 (функция с циклом внутри) — `range(n+1)` написал правильно с первого раза.
- [x] Урок 6 — списки (`list`) — ОСВОЕН (27–28.05.2026). Часть 1: индексы, `len()`, цикл по списку, накопитель. Часть 2: `append`, `pop`, изменение по индексу, оператор `in`. Все задания верны.
- [x] Урок 7 — словари (`dict`) — ОСВОЕН (31.05.2026). Теория + дрилл + цикл по словарю (`.items()`, `.values()`). Разогрев: функция `bonus_pool(deals, threshold)` с условным накопителем — написана самостоятельно, чисто.
- [x] Урок 8 — списки словарей — ОСВОЕН (01.06.2026). Задание по заказам — цикл по списку словарей, накопители, счётчик с условием. Комбинированная задача: функции `department_total` / `department_count` — код чистый и идиоматичный.
- [ ] **Урок 9** — следующий по плану

## Открытое домашнее задание
- (закрыто 20.05.2026) — Урок 4a ДЗ сделан, можно двигаться к 4b.

## Журнал ошибок (что прокачать)
- Урок 3, Задание 1: путал `or` и `and` при проверке диапазона. Запомнил: `and` — для "от и до", `or` — когда достаточно одного условия.
- Урок 3, Задание 2: использует обычный `print("...")` вместо `print(f"...")` для информативного вывода. Привычка использовать f-строки для отладочного вывода — ещё не выработана.
- Урок 3.5 (пересборка), Задание 5: захардкодил имя "Артём" буквами вместо переменной `{manager_name}`. Классическая новичковая ловушка — «не верь глазам, верь переменным». Исправил после объяснения, тест с подменой переменной прошёл.
- PEP 8: пробел вокруг операторов сравнения. Урок 4a, Задание 1: первый прогон `n >=1` и `n -=1` без пробела перед оператором. Замечание услышал, в Заданиях 2 и 3 ВЫРАБОТАЛ — везде корректно. Закрыто.
- Урок 4a, Задание 2: использовал `print(f"План выполнен!")` — f-префикс без переменных внутри `{}`. Не ошибка, но избыточно. f ставится только когда есть подстановка.
- Урок 4a ДЗ, Задача 2 (20.05.2026): `sales * 1.5` вместо `* 0.15` — в реальной системе менеджер получил бы в 10 раз больше. **Корневая причина:** не запустил код с примером из ТЗ и не сверил вывод с эталоном до сдачи. Привычка к ставить: **«написал → запустил → сверил с примером из ТЗ глазами → потом сдаю»**.
- Урок 4a ДЗ, Задача 1 (20.05.2026): регресс по PEP 8 (`n <=100` без пробела), хотя на 4a уже было выработано. Исправил после замечания. Следить — если повторится в 4b, ставим линтер.
- Урок 4a ДЗ, Задача 1 (20.05.2026): отклонился от спецификации текста вывода (написал «Сумма чисел от 1 до 100:» вместо «Сумма от 1 до 100 равна»). Не баг, но в работе ТЗ = контракт. Тренировать дисциплину «делаю как в задании», а не «как мне нравится».
- Урок 4b, Задача 1 (20.05.2026): переусложнил решение таблицы умножения (`n = 0`, `i *= 7` плюс параллельный счётчик). Решение работает, но не видит, что переменная цикла из `range` УЖЕ даёт нужное число. Признак: не доверяет встроенным механизмам Python, пытается «дублировать» работу языка. Лечится повторением и просмотром чужого простого кода.
- Урок 4b, Задача 2 (20.05.2026): `range(1, 101, 2)` для «чётных» — на самом деле даёт нечётные (1, 3, 5...). Получил 2500 вместо 2550, не заметил пока не сверил с эталоном. Снова та же дисциплина — запускать и сверять.
- Урок 4b, Задача 3 (20.05.2026): не перенёс паттерн накопителя (`total = 0` ДО цикла, потом `total += ...`) из `while` (4a) в `for`. Паттерн идентичный, но при смене типа цикла потерялся. Это **проблема переноса знаний**, не базового понимания. Лечится: в каждом следующем уроке с накопителем явно проговаривать «помнишь шаблон с 4a/4b?»
- Урок 4b, Задача 3 (20.05.2026): первая эмоциональная стена («впизду, не понимаю»). Прошёл через неё с помощью пошагового упражнения (мини-цикл с таблицей итераций). Реакция здоровая — написал «не понимаю» вместо сдачи кривого кода. Это важный поведенческий маркер, его и нужно подкреплять в дальнейшем.
- Урок 5, Задачи 1–3 (26.05.2026): избыточные промежуточные переменные в простых функциях (`revenue = price * quantity; return revenue` вместо `return price * quantity`). Не ошибка, но консервативный паттерн — «не доверяет, что можно сразу вернуть». Аналогично Уроку 4b.
- Урок 5 (26.05.2026): PEP 8 регресс — пропущены пробелы после запятых в аргументах функций: `calculate_revenue(1500,4)`. Периодически всплывает.
- Урок 7 (31.05.2026): `print('Бонусы менеджеров:')` написан до определения словаря `bonuses`. Не баг (вывод корректен), но необычный порядок кода — сначала заголовок, потом данные. Небольшая проблема с читаемостью.
- Урок 7 (31.05.2026): PEP 8 — `for name,percent` без пробела после запятой в распаковке. Периодическая проблема.
- Урок 8 (01.06.2026): `print(f"Список заказов:")` — f-строка без подстановки переменных (та же привычка что на Уроке 4a). Третье появление — стоит закрепить правило окончательно.
- Разогрев перед Уроком 9, Задача 4 (08.06.2026): прошёл список `orders` **двумя** циклами (один — печать, второй — счёт) вместо одного. Решение верное, но двойной проход по тем же данным. Объяснил принцип «один проход — все действия сразу» → переписал в один цикл.
- Закрепление, `training.py` (08.06.2026): классический баг области видимости — `total = 0` вне функции, но присваивание внутри → переменная локальная, не инициализирована. Нашёл и починил **сам** (перенёс `total = 0` внутрь функции). Это была самая сложная ошибка сессии.
- Закрепление, `training.py` (08.06.2026): путал круглые `()` и квадратные `[]` скобки — написал `total_won['status']` при вызове функции. Разобрали: `()` = вызвать функцию (внутри ингредиенты), `[]` = залезть в словарь за ключом. После этого вызвал правильно — `total_won(deals)`.
- Закрепление, `training.py` (08.06.2026): редундантный `float(total / count)` — оператор `/` в Python и так всегда возвращает float. Не баг. Запомнить: `/` → float, `//` → int.
- **Закрепление, `training.py`, задача №4 на `.append()` (08.06.2026): вторая выраженная эмоциональная стена** («блядь, я тупой», «нихера не понимаю», попросил написать код за него). **Корневая причина — усталость, не пробел в знаниях**: 23:00, ~час непрерывной работы, скелет функции (`def`/`for`/`if`/`return`) написал верно, забыл только `result = []` и название метода `.append()` (полу-вспомнил — «что-то с app...»). Дал код как обучающий артефакт + жёстко закрыл сессию. **Вывод для подачи:** разделять «забыл синтаксис/название метода» (норма, гуглится всю жизнь) и «не понимаю логику» (важно) — это снимает спираль «я тупой». И: вечерние сессии ограничивать ~часом, заканчивать на победе ДО стены.

## Достижения

### Урок 4a (19.05.2026)
- Первая задача с двумя переменными разной роли в цикле (счётчик `n` + накопитель `total`). Понял разделение с первого раза.
- Третье задание (сумма 1..100) сделал без подсказки — правильный старт `total = 0`, правильное условие `n <= 100` (включительно), правильный вынос `print` за пределы цикла.
- За одну сессию ПЕРЕВЕЛ привычку PEP 8-пробелов из «забывает» в «делает автоматически».

### Урок 4b (20.05.2026)
- **Прошёл через первую эмоциональную стену.** Написал «не понимаю», не сдал кривое, дал себя завести по шагам, дошёл до рабочего кода. Это критический поведенческий навык — выработался на 6-м уроке, раньше среднего.
- Самостоятельно диагностировал баги в своём коде (по моей просьбе «запусти и сверь») — оба пункта: «нет разбивки по дням, не верный итог». Раньше я указывал на баги сам.
- Самостоятельно вывел формулу плана (`50000 + (day * 10000 - 10000)`) через таблицу цифр, без подсказок к самой формуле.

### Достижения Урок 5 (26.05.2026)
- Все 4 задачи решены самостоятельно без единой подсказки — первый раз за обучение.
- Задача 4 (функция с циклом внутри): `range(n+1)` написал правильно с первого раза — паттерн диапазона перенёс из прошлого урока корректно.
- Паттерн накопителя внутри функции освоен как естественный.

### Достижения Урок 6 (27–28.05.2026)
- Использовал `deals[-1]` для последнего элемента — идиоматичный Python, не `deals[4]`.
- Встроил булево выражение в f-строку: `{'Сидоров' in clients}` — растёт инженерная интуиция.

### Достижения Урок 7 (31.05.2026)
- Функция `bonus_pool(deals, threshold)` — самостоятельно написал комбинацию функция + цикл + условный накопитель без подсказок.
- Правильно применил `.items()` и `.values()` в первый раз с первого раза.

### Достижения Урок 8 (01.06.2026)
- Комбинированная задача (`department_total` / `department_count`): две функции — чистый, идиоматичный код. Паттерн «функция как абстракция над данными» освоен — это фундамент FastAPI.
- Значительный рост качества кода по сравнению с Уроком 4b: нет переусложнений, структура понятная.

### Достижения Сессия 08.06.2026 (разогрев + закрепление перед Уроком 9)
- **Разогрев `lesson08_warmup.py` ЗАВЕРШЁН** — все 4 задачи, вывод 1-в-1 с эталонами.
- **Прорыв по уверенности.** Пришёл с установкой «мне просто везёт писать код». Корень — не было модели выполнения кода в голове. Разобрали принцип **«нельзя использовать то, чего ещё нет»** (порядок строк = зависимость по данным) + аналогию «функция = рецепт в книге» (что внутри / что снаружи). Прокрутил цикл-накопитель руками **без ошибок** (увидел, что `total` тащит итог между проходами — самое сложное место). Сам объяснил порядок выполнения функции, включая «прыжок внутрь и возврат».
- **Собрал 4 функции со скелетом «функция + список словарей + накопитель + return + вызов(аргумент)»** в `training.py`: `total_won` (debug сам + Сократ по вызову), `count_cheap` (соло, без подсказок), `average_expensive` (ДВА накопителя + деление — сложную часть взял сам), `get_expensive_names` (на `.append()` — упёрся в стену усталости, дал код).
- **Сам заметил разницу** между «запомнил позиции» и «понимаю». Дал ему задачу №3 (среднее) специально такую, что старый шаблон НЕ ложится — собрал всё равно. Это и доказало переход от зубрёжки к пониманию.

- [x] Урок 9 — обработка ошибок (`try` / `except`) — ОСВОЕН (09.06.2026). Файл: `lesson09_errors.py`. Базовый случай (`int(input())` + `except ValueError`), `pass` как «пропустить», try/except внутри цикла по списку с мусором (сумма валидных = 60). Обе задачи решены самостоятельно (спросил только название слова `pass` — новый синтаксис, не проходили).

### Достижения Урок 9 (09.06.2026)
- **Дисциплина «запусти → сверь» сработала ВЖИВУЮ.** В задаче 1 сначала написал `f'{int(num)}*2'` (звёздочка снаружи скобок → печатало бы «5*2»). Запустил, сверил с эталоном «10», увидел расхождение, исправил сам на `{int(num) * 2}`. Это ровно тот тип бага, что на 4a/4b уходил в сдачу. Дыра №1 закрыта на практике.
- **Синтез старого и нового скелета.** Встроил try/except внутрь привычного цикла-накопителя по списку без запинки. Перенос знаний между паттернами (вчерашняя слабая зона) сегодня сработал.
- Правильно понял, что `input()` нужно держать ВНЕ `try` — падает не ввод, а `int()`. Чувствует, где именно может сломаться код.
- Warmup в начале сессии (`total_payroll`) — чистое соло с первой попытки через сутки после «я ничего не помню». Факт против самооценки.

- [x] Урок 10 — ПЕРВЫЙ МИНИ-ПРОЕКТ «Генератор отчёта по сделкам» — ЗАВЕРШЁН (14.06.2026). Файл: `lesson10_report.py`. Сборка всего: функция над списком словарей + фильтр по статусу + `try/except` на «грязные» данные (строки/пустые/мусор как из CRM) + один проход считает выручку И количество + распаковка пары + `round` + форматированный отчёт. Вывод 1-в-1 с эталоном (430000, 3, средний чек 143333.33). Разогревы: `safe_sum`, `count_done`.

### Достижения Урок 10 — первый мини-проект (14.06.2026)
- **Первый завершённый артефакт.** Не упражнение, а маленькая настоящая программа: грязные данные → фильтр → бизнес-отчёт. Прямая основа маяка (агрегатор вакансий = та же обработка данных).
- **Дизайн-мышление.** Сам спросил «писать отдельную функцию для count или встроить в существующую?» — мышление инженера, не новичка. Выбрал эффективный вариант (один проход, `return total, count`).
- **Тонкость взял сам:** поставил `count += 1` ВНУТРИ `try` после `int()` — поэтому считаются только сделки с валидной суммой (пустая Орлова отсекается). Вывел без подсказки.
- Учёл всю обратную связь (`except ValueError` вместо голого, убрал `else: pass`, переименовал функцию). Освоил 3 новых инструмента за вечер: распаковка пары, `round(x, 2)`, многострочный вывод.
- **Дисциплина «сверь построчно» снова сработала** — нашли расхождения с эталоном (нет `руб.`, лишние пробелы), и опечатку `во`, влезшую ПРИ правке. Закрепили правило: правка может родить новый баг → перепроверяй после КАЖДОГО изменения.
- Эмоц. контекст: сделал это вечером в воскресенье перед тяжёлым понедельником, на фоне рабочего стресса. Не отложил. Связали урок с его «зачем» (нормальная жизнь для семьи).

- [x] Урок 11 — ООП, Часть 1 (классы/объекты/атрибуты) — ОСВОЕН (15.06.2026). Файл: `lesson11_oop.py`. класс=чертёж / объект=вещь; `Name()`, атрибуты через точку.
- [x] Урок 11 — ООП, Часть 2 (`__init__` + `self`) — ОСВОЕН (16.06.2026). Файлы: `lesson11_oop.py`, `lesson11_practice.py`. Прошёл через «кашу» до понимания: `self` = сама коробка (объект), а не значения внутри; `__init__` = сборщик, заполняет поля при рождении. Закрепил двумя объектами (увидел независимые коробки от одного класса). 3 практические задачи соло (Product / список Student'ов / список Deal + накопитель). Открыл сам: список хранит любые типы, в т.ч. объекты.

### Достижения Урок 11 (15–16.06.2026)
- **Взял `self`/`__init__`** — самый сложный концепт ООП — за один вечер, через осознанную «кашу», не сдавшись.
- **Зрелая модель обучения (ключевой маркер).** Сам сформулировал: «каша сейчас — нормально, будет как со списками, нужна только тренировка». Раньше «не понимаю» = «я тупой» (спираль). Теперь «не понимаю» = «этап, осядет через практику». Это сдвиг мышления уровнем выше синтаксиса — подкреплять.
- **Сам просит практику** и решает с нарастанием сложности. Перенёс паттерн-накопитель на список ОБЪЕКТОВ (доступ через точку вместо `[]`).
- Скобки: путал `{}` (set) и `[]` (list) в Задаче 3 — вышло 430000 «по везению» (сумма не зависит от порядка). Разобрали: set молча теряет дубликаты — для денег опасно. Поправил на `[]`. (Перекликается с путаницей `()`/`[]` от 08.06 — типы скобок прибиваем повторами.)

- [x] Урок 11 — ООП, Часть 3 (методы) — ОСВОЕН (17.06.2026). Файлы: `lesson11_methods.py`, `lesson11_methods_practice.py`. Метод = функция внутри класса с `self`; вызов `obj.method()` (со скобками — действие) vs `obj.field` (атрибут — без скобок). Разобрали «обычная функция vs метод» (метод знает свои данные через self, передавать не надо). Практика — 3 грани методов: (1) читает свои данные (Employee.yearly), (2) метод с доп. параметром + решение (BankAccount.can_afford(price) — сам распутал 5 багов), (3) метод меняет состояние (Counter.increment → self.count += 1; + `__init__` без доп. параметров). Финальный баг — вывел `c.increment` (метод) вместо `c.count` (атрибут) — отличная иллюстрация различия, поправил.

### Достижения Урок 11, методы (17.06.2026)
- Заметил САМ, что `self.client = client` лишняя для задачи, где client не используется — инженерное «а нужна ли эта строка?», не копирование.
- В BankAccount распутал 5 наложившихся багов самостоятельно (опечатка, несуществующее поле, нехватка аргументов, пустой вызов, недостающая проверка) — навык отладки.
- **Осадил меня по методике:** задачу 3 я дал с непоказанными концептами (`__init__` без параметров, мутация состояния). Он справедливо возразил «зачем даёшь то, что не объяснил». Признал косяк, дал пример ПЕРЕД задачей. Маркер: у него есть стандарты к процессу, держать «сначала покажи — потом спрашивай».

- [x] Мини-проект CRM (18.06.2026) — `mini_crm.py`: класс `Deal` + класс `CRM` (хранит список сделок, `add_deal`, `report` по закрытым). Собрал рабочую программу из 2 классов, НО к концу упёрся в эмоциональную стену («ничего не понимаю, просто срисовал»). Корень — усталость в 22:00 + поспешил из «вакуума» в проект, недотренировав основы. Попросил переучить ООП с нуля на других примерах.

### Сессия 21.06.2026 — переучивание ООП после перерыва (СИЛЬНЫЙ ВОССТАНОВИТЕЛЬНЫЙ ЗАХОД)
- Вернулся после 3-дневного перерыва (отдых + встретился с другом — здоровая пауза). Зашёл с остаточной «кашей», но сессия прошла отлично.
- Переучили ООП мягко, на НОВОМ примере (класс `Cat`, не бизнес) — `oop_warmup.py`. Ключевое: **вёл через его вопросы**, а не лекциями. Он сам рассуждал вслух и сам выводил/исправлял:
  - сам поправил «self = данные» → «self = коробка, в ней лежат значения»;
  - сам вывел «класс = КАКИЕ данные, метод = ЧТО с ними делаем»;
  - сам расцепил два момента: позиционные аргументы при СОЗДАНИИ `Cat("Барсик",3)` vs метод просто ЧИТАЕТ готовые данные при вызове;
  - сам добавил ВТОРОГО кота и проверил, что `self` работает для каждого объекта;
  - заметил проблему склонений (имя хранится в одной форме, код не знает грамматику) — глаз инженера.
- **Соло-задача без подсказок** (`solo_task.py`, класс `Product` + метод `with_discount`) — сам предложил, с настроем «не выйдет — пофигу, не расстроюсь». РЕШИЛ. Предсказал ошибку НЕ запуская код (think-like-computer). Сам разрулил `self.percent`→`percent`. Передал 0.2 вместо 20 — рабочее решение.
- **Ключевые маркеры зрелости:** (1) «не выйдет — пофигу» = отцепил самооценку от результата (антидот к спирали «я тупой»); (2) сам чувствует «сегодня лучше»; (3) отдых реально помог — его же теория «каша осядет через паузу» подтвердилась.

### Сессия 23.06.2026 — «думать как программист» (5-шаговая схема) + соло-задачи
- **Главное:** сам попросил научить **рассуждать как программист**, а не копировать. Дали **схему из 5 шагов**: 1) вещь→класс; 2) данные (передаю vs фиксировано); 3) метод (ЧИТАЕТ или МЕНЯЕТ?); 4) вывод; 5) сценарий.
- Прошёл по схеме и решил `solo_task3` (Parcel/доставка), `solo_task4` (Thermostat), `solo_task5` (Worker/зарплата), `solo_task6` (Phone/заряд). База (класс, `__init__`, тело метода) идёт всё самостоятельнее.
- **Слабое место выявлено и добиваем:** «читатель vs изменитель» в ИСПОЛЬЗОВАНИИ — где число берётся из ВЫЗОВА метода (reader+return), а где из ПОЛЯ объекта (changer, читаем атрибут). На Phone он это место взял сам (прочитал поле в print).
- **Зрелость:** сам заметил «опять просто копирую с термостата». Разобрали честно: распознать общий паттерн за разными задачами (термостат↔телефон) — это перенос/абстракция, а НЕ слепое копирование; но его инстинкт «хочу, чтобы тянули вперёд, а не по кругу» — верный, дальше давать НОВЫЕ паттерны.
- Создан `MY_GOAL.md` (в репозитории) — его маяк: инженерия+AI, не продажи, точка входа junior Python+LLM, честно про рынок, его «зачем» (семья).

### Следующие шаги
- [ ] **Новый паттерн (НЕ ре-скин):** метод, принимающий РЕШЕНИЕ на основе своих данных (`if` внутри метода). Брать на свежую голову. Дальше — комбинации (reader+changer в одном классе), потом мини-CRM снова / месяц 3 FastAPI.
- **Правила процесса (его обратная связь 23.06, ВАЖНО):** (1) при подсказках — НЕ писать ему готовый код, только словами/вопросами, код даёт он сам, пока прямо не попросит; (2) **читать его АКТУАЛЬНЫЙ код перед каждым ответом** — в этой сессии я путал его, ссылаясь на устаревшие версии файла. Быть внимательным.

---

## 🔄 СТРАТЕГИЧЕСКОЕ РЕШЕНИЕ (20.05.2026)

После разговора об актуальности рынка джунов под AI Руслан выбрал **переразверстать план под рынок 2026**, а не идти по «классической дорожной карте 2023».

**Целевая ниша:** product-engineer с AI-интеграциями для **РФ-рынка** (не международного). Под санкции и реальные ограничения международного найма российских разработчиков.

**Целевой стек (финальный):**
- Python → FastAPI → PostgreSQL → Docker
- **+ LLM-интеграция** — конкретный провайдер (YandexGPT / GigaChat / OpenAI / open-source) выбираем на **месяце 5**, по факту состояния рынка. Слот зарезервирован, выбор отложен.

**Что меняется в плане:**
1. **Месяцы 1–2 не трогаем** — Python-основы и функции нужны в любом случае.
2. **Месяцы 3–4 — переразверстать**: вместо «структуры данных + ООП + файлы» сразу FastAPI + базовый CRUD + первый сервис, дёргающий любой внешний HTTP-API (универсальный навык, не привязка к конкретному LLM).
3. **Месяц 5 — выбор LLM-провайдера**: даю свежий анализ рынка, Руслан выбирает под реальную картинку.
4. **Появляется правило артефактов**: каждый месяц = один рабочий проект, который можно показать. Портфолио строится с нуля.
5. **Сжатие срока**: 12–14 мес → 8–10 мес. реалистично при текущей регулярности.

**По английскому (уточнено 20.05.2026):**
- ❌ Разговорный английский для интервью — **отменён** (РФ-рынок не требует)
- ✅ Технический английский на чтение — **встроен в уроки** (доки на английском подаются порциями, без отдельного трека)

**Что НЕ меняется:**
- Правило «писать код руками, не через Claude» — критично, т.к. под AI-рынок важно понимать и править чужой/сгенерированный код.
- Качество > скорость на каждом отдельном уроке.
- Подача через теорию + аналогии + самостоятельные задачи.

**Принцип:** late binding — не фиксировать решения раньше, чем они становятся связывающими. Учить **универсальные навыки** (работа с API в принципе), не привязки к конкретным сервисам.

**TODO (для Клода):**
- [ ] Переписать «Дорожную карту» в виде сравнения старый/новый план
- [ ] Подобрать первый артефакт месяца 1 (вероятно: калькулятор бизнес-метрик в консоли, использующий всё из недели 1–4)
- [ ] К месяцу 4: подготовить сравнительный анализ LLM-API доступных в РФ-периметре

---
*Обновляется после каждого урока*
